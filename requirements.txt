# Root requirements.txt intentionally minimal.
# The API function uses api/requirements.txt (anthropic only).
# For running the Streamlit local version, use: pip install streamlit reportlab pandas anthropic
